Pasos a seguir para levantar una Instancia de Airflow en un contenedor Docker:
1. Instalar docker-compose.ymel.
2. Inicializar (docker-compose up airflow init).
3. Levantar en un localhost (docker-compose up).
